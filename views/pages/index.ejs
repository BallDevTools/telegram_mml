<!-- views/pages/index.ejs - Complete 100% -->
<div class="homepage">
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-content">
            <div class="hero-icon">🚀</div>
            <h1 class="hero-title">Crypto Membership NFT</h1>
            <p class="hero-subtitle">
                Join the exclusive decentralized membership ecosystem. 
                Earn commissions, collect unique NFTs, and build your network.
            </p>
            
            <div class="hero-stats">
                <div class="stat">
                    <div class="stat-number"><%= (typeof systemStats !== 'undefined' && systemStats) ? parseInt(systemStats.totalMembers) : 0 %></div>
                    <div class="stat-label">Members</div>
                </div>
                <div class="stat">
                    <div class="stat-number">16</div>
                    <div class="stat-label">Levels</div>
                </div>
                <div class="stat">
                    <div class="stat-number">BSC</div>
                    <div class="stat-label">Network</div>
                </div>
            </div>

            <div class="hero-actions">
                <% if (typeof userMembership !== 'undefined' && userMembership) { %>
                    <a href="/membership/dashboard" class="cta-button primary">
                        <span class="btn-icon">📊</span>
                        <span>Dashboard</span>
                    </a>
                    <a href="/membership/plans" class="cta-button secondary">
                        <span class="btn-icon">⬆️</span>
                        <span>Upgrade</span>
                    </a>
                <% } else { %>
                    <a href="/membership/plans" class="cta-button primary">
                        <span class="btn-icon">🎯</span>
                        <span>Get Started</span>
                    </a>
                    <a href="/wallet/connect" class="cta-button secondary">
                        <span class="btn-icon">🔗</span>
                        <span>Connect Wallet</span>
                    </a>
                <% } %>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features-section">
        <h2>Why Choose Our Platform?</h2>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">🎨</div>
                <h3>Unique NFTs</h3>
                <p>Receive exclusive non-transferable NFT membership cards that represent your level and achievements.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">💰</div>
                <h3>Earn Commissions</h3>
                <p>Generate passive income through our referral system. Higher levels unlock better commission rates.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🔒</div>
                <h3>Secure & Transparent</h3>
                <p>Built on Binance Smart Chain with full transparency. All transactions are verifiable on-chain.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">📈</div>
                <h3>Growth Potential</h3>
                <p>16 membership levels from $1 to $16. Upgrade progressively and unlock new opportunities.</p>
            </div>
        </div>
    </section>

    <!-- Plans Preview -->
    <section class="plans-preview">
        <h2>Membership Plans</h2>
        <p class="section-subtitle">Choose from 16 different membership levels</p>
        
        <div class="plans-grid">
            <div class="plan-preview-card starter">
                <div class="plan-tier">
                    <span class="tier-badge starter">Starter</span>
                </div>
                <div class="plan-name">Starter</div>
                <div class="plan-level">Level 1</div>
                <div class="plan-price">$1</div>
                <div class="plan-features">
                    <div class="feature">✓ NFT Membership Card</div>
                    <div class="feature">✓ Referral System</div>
                    <div class="feature">✓ Basic Rewards</div>
                </div>
            </div>
            
            <div class="plan-preview-card premium">
                <div class="plan-tier">
                    <span class="tier-badge premium">Premium</span>
                </div>
                <div class="plan-name">Gold</div>
                <div class="plan-level">Level 5</div>
                <div class="plan-price">$5</div>
                <div class="plan-features">
                    <div class="feature">✓ Enhanced Commissions</div>
                    <div class="feature">✓ Priority Support</div>
                    <div class="feature">✓ Bonus Rewards</div>
                </div>
            </div>
            
            <div class="plan-preview-card elite">
                <div class="plan-tier">
                    <span class="tier-badge elite">Elite</span>
                </div>
                <div class="plan-name">Diamond</div>
                <div class="plan-level">Level 7</div>
                <div class="plan-price">$7</div>
                <div class="plan-features">
                    <div class="feature">✓ Maximum Commissions</div>
                    <div class="feature">✓ VIP Access</div>
                    <div class="feature">✓ Exclusive Benefits</div>
                </div>
            </div>
            
            <div class="plan-preview-card infinity">
                <div class="plan-tier">
                    <span class="tier-badge infinity">Infinity</span>
                </div>
                <div class="plan-name">Infinity</div>
                <div class="plan-level">Level 16</div>
                <div class="plan-price">$16</div>
                <div class="plan-features">
                    <div class="feature">✓ Ultimate Status</div>
                    <div class="feature">✓ Premium Rewards</div>
                    <div class="feature">✓ Leadership Benefits</div>
                </div>
            </div>
        </div>
        
        <div class="plans-action">
            <a href="/membership/plans" class="view-all-plans">
                View All 16 Plans →
            </a>
        </div>
    </section>

    <!-- How It Works -->
    <section class="how-it-works">
        <h2>How It Works</h2>
        <p class="section-subtitle">Get started in 4 simple steps</p>
        
        <div class="steps-container">
            <div class="step">
                <div class="step-number">1</div>
                <div class="step-content">
                    <h3>Connect Wallet</h3>
                    <p>Connect your BSC-compatible wallet like MetaMask or Trust Wallet</p>
                </div>
            </div>
            
            <div class="step">
                <div class="step-number">2</div>
                <div class="step-content">
                    <h3>Choose Plan</h3>
                    <p>Select from 16 membership levels starting from just $1 USDT</p>
                </div>
            </div>
            
            <div class="step">
                <div class="step-number">3</div>
                <div class="step-content">
                    <h3>Get NFT</h3>
                    <p>Receive your unique membership NFT and start earning</p>
                </div>
            </div>
            
            <div class="step">
                <div class="step-number">4</div>
                <div class="step-content">
                    <h3>Refer & Earn</h3>
                    <p>Share your referral link and earn commissions from new members</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats-section">
        <h2>Platform Statistics</h2>
        <div class="stats-grid-large">
            <div class="stat-card">
                <div class="stat-icon">👥</div>
                <div class="stat-number"><%= (typeof systemStats !== 'undefined' && systemStats) ? parseInt(systemStats.totalMembers) : 0 %></div>
                <div class="stat-label">Total Members</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">💰</div>
                <div class="stat-number">$<%= (typeof systemStats !== 'undefined' && systemStats) ? (parseInt(systemStats.totalRevenue) / 1000000).toFixed(0) : 0 %>K</div>
                <div class="stat-label">Total Volume</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">🎯</div>
                <div class="stat-number">16</div>
                <div class="stat-label">Membership Levels</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">🔗</div>
                <div class="stat-number">BSC</div>
                <div class="stat-label">Blockchain</div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-content">
            <h2>Ready to Start Your Journey?</h2>
            <p>Join thousands of members who are already earning and growing their network</p>
            
            <% if (typeof userMembership === 'undefined' || !userMembership) { %>
                <div class="cta-buttons">
                    <a href="/membership/plans" class="cta-button primary large">
                        Start Now - From $1
                    </a>
                    <a href="/wallet/connect" class="cta-button secondary large">
                        Connect Wallet First
                    </a>
                </div>
            <% } else { %>
                <div class="member-cta">
                    <div class="current-level">
                        <span>Your Current Level: <strong><%= userMembership.planName %></strong></span>
                    </div>
                    <a href="/membership/dashboard" class="cta-button primary large">
                        View Dashboard
                    </a>
                </div>
            <% } %>
        </div>
    </section>

    <!-- Debug Info for Development -->
    <% if (process.env.NODE_ENV === 'development') { %>
        <section class="debug-section">
            <div class="debug-card">
                <h3>🔧 Debug Information</h3>
                <div class="debug-info">
                    <p><strong>Telegram User:</strong> <%= (typeof telegramUser !== 'undefined' && telegramUser) ? 'Connected ✅' : 'Not connected ❌' %></p>
                    <% if (typeof telegramUser !== 'undefined' && telegramUser) { %>
                        <p><strong>User Name:</strong> <%= telegramUser.first_name %> <%= telegramUser.last_name || '' %></p>
                        <p><strong>Username:</strong> @<%= telegramUser.username || 'N/A' %></p>
                        <p><strong>Language:</strong> <%= telegramUser.language_code || 'N/A' %></p>
                        <p><strong>Theme:</strong> <%= telegramUser.colorScheme || 'light' %></p>
                    <% } %>
                    <p><strong>User Data:</strong> <%= (typeof user !== 'undefined' && user) ? 'Available ✅' : 'Not available ❌' %></p>
                    <p><strong>Environment:</strong> <%= process.env.NODE_ENV %></p>
                    <p><strong>Timestamp:</strong> <%= new Date().toLocaleString() %></p>
                </div>
                
                <div class="debug-links">
                    <a href="/health" class="debug-link">Health Check</a>
                    <a href="/test-telegram" class="debug-link">Telegram Test</a>
                    <a href="/api/health" class="debug-link">API Health</a>
                    <a href="/test-database" class="debug-link">Database Test</a>
                </div>
            </div>
        </section>
    <% } %>
</div>

<style>
.homepage {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

.hero-section {
    text-align: center;
    padding: 60px 0;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 20px;
    margin-bottom: 60px;
    color: white;
    position: relative;
    overflow: hidden;
}

.hero-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.1) 50%, transparent 70%);
    pointer-events: none;
}

.hero-content {
    position: relative;
    z-index: 2;
}

.hero-icon {
    font-size: 4rem;
    margin-bottom: 20px;
    animation: float 3s ease-in-out infinite;
}

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
}

.hero-title {
    font-size: 3rem;
    font-weight: bold;
    margin-bottom: 16px;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
}

.hero-subtitle {
    font-size: 1.2rem;
    margin-bottom: 40px;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    opacity: 0.9;
    line-height: 1.6;
}

.hero-stats {
    display: flex;
    justify-content: center;
    gap: 40px;
    margin-bottom: 40px;
}

.stat {
    text-align: center;
    padding: 20px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.stat-number {
    font-size: 2rem;
    font-weight: bold;
    margin-bottom: 5px;
}

.stat-label {
    font-size: 0.9rem;
    opacity: 0.8;
}

.hero-actions {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
}

.cta-button {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 24px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
}

.cta-button.primary {
    background: #28a745;
    color: white;
}

.cta-button.secondary {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
}

.cta-button.large {
    padding: 16px 32px;
    font-size: 1.1rem;
}

.cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
}

.section-subtitle {
    text-align: center;
    color: var(--tg-theme-hint-color);
    margin-bottom: 30px;
    font-size: 1.1rem;
}

.features-section {
    padding: 60px 0;
    text-align: center;
}

.features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 30px;
    margin-top: 40px;
}

.feature-card {
    padding: 30px 20px;
    border-radius: 12px;
    border: 1px solid #e1e5e9;
    transition: all 0.3s ease;
    background: var(--tg-theme-bg-color);
}

.feature-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    border-color: var(--tg-theme-button-color);
}

.feature-icon {
    font-size: 2.5rem;
    margin-bottom: 20px;
}

.plans-preview {
    padding: 60px 0;
    background: var(--tg-theme-secondary-bg-color);
    border-radius: 20px;
    margin: 40px 0;
    text-align: center;
}

.plans-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    margin: 40px 0;
    padding: 0 20px;
}

.plan-preview-card {
    padding: 24px;
    border-radius: 12px;
    border: 2px solid transparent;
    transition: all 0.3s ease;
    background: var(--tg-theme-bg-color);
    text-align: center;
}

.plan-preview-card.starter {
    border-color: #ffeaa7;
}

.plan-preview-card.premium {
    border-color: #a29bfe;
}

.plan-preview-card.elite {
    border-color: #fd79a8;
}

.plan-preview-card.infinity {
    border-color: #00b894;
}

.plan-preview-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
}

.tier-badge {
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 500;
    margin-bottom: 15px;
    display: inline-block;
}

.tier-badge.starter { background: #ffeaa7; color: #d63031; }
.tier-badge.premium { background: #a29bfe; color: white; }
.tier-badge.elite { background: #fd79a8; color: white; }
.tier-badge.infinity { background: #00b894; color: white; }

.plan-name {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 5px;
}

.plan-level {
    color: var(--tg-theme-hint-color);
    font-size: 0.9rem;
    margin-bottom: 15px;
}

.plan-price {
    font-size: 1.8rem;
    font-weight: bold;
    color: var(--tg-theme-button-color);
    margin-bottom: 20px;
}

.plan-features {
    text-align: left;
}

.feature {
    padding: 4px 0;
    font-size: 0.9rem;
    color: var(--tg-theme-text-color);
}

.view-all-plans {
    display: inline-block;
    padding: 12px 24px;
    background: var(--tg-theme-button-color);
    color: var(--tg-theme-button-text-color);
    text-decoration: none;
    border-radius: 8px;
    font-weight: 500;
    transition: all 0.3s ease;
    margin-top: 20px;
}

.view-all-plans:hover {
    transform: translateY(-2px);
    opacity: 0.9;
}

.how-it-works {
    padding: 60px 0;
    text-align: center;
}

.steps-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 30px;
    margin-top: 40px;
    padding: 0 40px;
}

.step {
    text-align: center;
    position: relative;
}

.step::after {
    content: '';
    position: absolute;
    top: 25px;
    left: calc(100% + 15px);
    width: 30px;
    height: 2px;
    background: var(--tg-theme-button-color);
    z-index: -1;
}

.step:last-child::after {
    display: none;
}

.step-number {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: var(--tg-theme-button-color);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 1.2rem;
    margin: 0 auto 20px;
    box-shadow: 0 4px 10px rgba(36, 129, 204, 0.3);
}

.stats-section {
    padding: 60px 0;
    text-align: center;
}

.stats-grid-large {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 30px;
    margin-top: 40px;
}

.stat-card {
    padding: 30px 20px;
    background: var(--tg-theme-bg-color);
    border: 1px solid #e1e5e9;
    border-radius: 12px;
    text-align: center;
    transition: transform 0.3s ease;
}

.stat-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
}

.stat-icon {
    font-size: 2.5rem;
    margin-bottom: 15px;
}

.cta-section {
    padding: 80px 0;
    text-align: center;
    background: linear-gradient(135deg, #2d3748 0%, #4a5568 100%);
    border-radius: 20px;
    color: white;
    margin: 60px 0 40px;
}

.cta-buttons {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 30px;
}

.member-cta {
    margin-top: 30px;
}

.current-level {
    margin-bottom: 20px;
    font-size: 1.1rem;
}

.debug-section {
    margin: 40px 0;
    padding: 20px 0;
}

.debug-card {
    background: var(--tg-theme-secondary-bg-color);
    border-radius: 12px;
    padding: 20px;
    border: 1px solid var(--tg-theme-hint-color);
}

.debug-info {
    margin: 15px 0;
    font-family: monospace;
    font-size: 0.9rem;
    line-height: 1.6;
}

.debug-links {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 15px;
}

.debug-link {
    padding: 8px 16px;
    background: var(--tg-theme-button-color);
    color: var(--tg-theme-button-text-color);
    text-decoration: none;
    border-radius: 6px;
    font-size: 0.85rem;
    transition: all 0.2s ease;
}

.debug-link:hover {
    opacity: 0.8;
    transform: translateY(-1px);
}

@media (max-width: 768px) {
    .hero-title {
        font-size: 2rem;
    }
    
    .hero-stats {
        gap: 20px;
        flex-direction: column;
    }
    
    .hero-actions {
        flex-direction: column;
        align-items: center;
    }
    
    .features-grid,
    .plans-grid,
    .steps-container,
    .stats-grid-large {
        grid-template-columns: 1fr;
    }
    
    .steps-container {
        padding: 0 20px;
    }
    
    .step::after {
        display: none;
    }
    
    .cta-buttons {
        flex-direction: column;
        align-items: center;
    }
}

@media (max-width: 480px) {
    .homepage {
        padding: 0 16px;
    }
    
    .hero-section,
    .plans-preview,
    .cta-section {
        padding: 40px 20px;
    }
    
    .hero-title {
        font-size: 1.8rem;
    }
    
    .hero-subtitle {
        font-size: 1rem;
    }
    
    .plans-grid {
        padding: 0 10px;
    }
}
</style>

<script>
// Animate stats on scroll
function animateStats() {
    const stats = document.querySelectorAll('.stat-number');
    stats.forEach(stat => {
        const finalText = stat.textContent;
        const target = parseInt(finalText.replace(/[^0-9]/g, ''));
        
        if (!isNaN(target) && target > 0) {
            let current = 0;
            const increment = target / 50;
            
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    current = target;
                    clearInterval(timer);
                    stat.textContent = finalText;
                } else {
                    stat.textContent = Math.floor(current);
                }
            }, 30);
        }
    });
}

// Intersection Observer for animations
function setupScrollAnimations() {
    const animateElements = document.querySelectorAll('.feature-card, .plan-preview-card, .step, .stat-card');
    
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    });
    
    animateElements.forEach(el => {
        el.style.opacity = '0';
        el.style.transform = 'translateY(20px)';
        el.style.transition = 'all 0.6s ease';
        observer.observe(el);
    });
}

// Initialize animations
document.addEventListener('DOMContentLoaded', () => {
    setTimeout(animateStats, 500);
    setupScrollAnimations();
});

// Handle CTA button clicks with haptic feedback
document.querySelectorAll('.cta-button, .view-all-plans, .debug-link').forEach(button => {
    button.addEventListener('click', () => {
        if (window.Telegram?.WebApp?.HapticFeedback) {
            window.Telegram.WebApp.HapticFeedback.impactOccurred('light');
        }
    });
});

// Update stats periodically (if needed)
if (window.APP_DATA?.user) {
    setInterval(() => {
        // Could fetch updated stats here
        console.log('Stats update check...');
    }, 60000); // Every minute
}
</script>